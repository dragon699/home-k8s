---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: postgresql-shared
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
  annotations:
    env: home
spec:
  project: apps
  sources:
  - repoURL: git@github.com:dragon699/home-k8s.git
    targetRevision: main
    ref: values
  - repoURL: https://percona.github.io/percona-helm-charts/
    targetRevision: 2.7.0
    chart: pg-operator
  - repoURL: https://percona.github.io/percona-helm-charts/
    targetRevision: 2.7.0
    chart: pg-db
    helm:
      valueFiles:
      - '$values/helm/apps/postgresql-shared/values.yaml'
  - repoURL: git@github.com:dragon699/home-k8s.git
    targetRevision: main
    path: manifests/apps/postgresql-shared
    directory:
      recurse: true
  destination:
    server: https://kubernetes.default.svc
    namespace: shared
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
    - Validate=true
    - PrunePropagationPolicy=foreground
    - PruneLast=true
    - ServerSideApply=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 1m
