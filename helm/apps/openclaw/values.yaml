app-template:
  configMode: merge

  configMaps:
    config:
      enabled: true

  controllers:
    main:
      containers:
        main:
          envFrom:
            - secretRef:
                name: data-openclaw
