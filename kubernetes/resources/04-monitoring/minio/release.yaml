---
apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: minio
  namespace: monitoring
spec:
  interval: 5m
  chart:
    spec:
      chart: minio
      version: 14.7.0
      sourceRef:
        kind: HelmRepository
        name: bitnami
      interval: 5m0s
  valuesFrom:
  - name: values-minio
    kind: ConfigMap
