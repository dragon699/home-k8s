---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: data-glance
  namespace: glance
spec:
  refreshInterval: 5m
  secretStoreRef:
    name: home-vault
    kind: ClusterSecretStore
  target:
    name: data-glance
    creationPolicy: Owner
  dataFrom:
  - extract:
      key: home/glance/data-glance
      conversionStrategy: Default
      decodingStrategy: None
      metadataPolicy: None
