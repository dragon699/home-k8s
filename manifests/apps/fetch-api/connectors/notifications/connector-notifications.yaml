---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: connector-notifications
  namespace: fetch-api
  labels:
    app: connector-notifications
    env: home
spec:
  replicas: 1
  revisionHistoryLimit: 0
  selector:
    matchLabels:
      app: connector-notifications
  template:
    metadata:
      labels:
        app: connector-notifications
        env: home
    spec:
      securityContext:
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
      restartPolicy: Always
      imagePullSecrets:
        - name: registry-fetch-api
      containers:
        - name: connector-notifications
          image: ghcr.io/dragon699/home-k8s/fetch-api/connectors/notifications:0.0.2
          imagePullPolicy: Always
          securityContext:
            runAsUser: 1000
            runAsGroup: 1000
            runAsNonRoot: true
          ports:
            - containerPort: 9069
          command: ["/app/connector-notifications"]
          envFrom:
            - secretRef:
                name: data-connector-notifications
            - configMapRef:
                name: config-connector-notifications
