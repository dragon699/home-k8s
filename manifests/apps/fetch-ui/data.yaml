---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: registry-fetch-ui
  namespace: fetch-ui
spec:
  refreshInterval: 5m0s
  secretStoreRef:
    name: home-vault
    kind: ClusterSecretStore
  target:
    name: registry-fetch-ui
    creationPolicy: Owner
    template:
      type: kubernetes.io/dockerconfigjson
      data:
        .dockerconfigjson: |
          {
            "auths": {
              "ghcr.io": {
                "username": "{{ .username }}",
                "password": "{{ .password }}",
                "email": "{{ .email }}",
                "auth": "{{ printf "%s:%s" .username .password | b64enc }}"
              }
            }
          }
  dataFrom:
  - extract:
      key: home/fetch-ui/registry-fetch-ui
      conversionStrategy: Default
      decodingStrategy: None
      metadataPolicy: None

---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: auth-fetch-ui
  namespace: fetch-ui
spec:
  refreshInterval: 5m0s
  secretStoreRef:
    name: home-vault
    kind: ClusterSecretStore
  target:
    name: auth-fetch-ui
    creationPolicy: Owner
  dataFrom:
  - extract:
      key: home/fetch-ui/auth-fetch-ui
      conversionStrategy: Default
      decodingStrategy: None
      metadataPolicy: None
