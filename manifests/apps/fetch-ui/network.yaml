---
apiVersion: v1
kind: Service
metadata:
  name: fetch-ui
  namespace: fetch-ui
  labels:
    env: home
spec:
  type: ClusterIP
  ports:
  - name: fetch-ui
    port: 80
    targetPort: 80
  selector:
    app: fetch-ui

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: fetch-ui
  namespace: fetch-ui
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-home
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/auth-type: basic
    nginx.ingress.kubernetes.io/auth-secret: auth-fetch-ui
    nginx.ingress.kubernetes.io/auth-realm: "Authentication Required"
  labels:
    app: fetch-ui
    env: home
spec:
  tls:
  - hosts:
    - control.k8s.iaminyourpc.xyz
    secretName: tls-control-k8s-iaminyourpc-xyz
  rules:
  - host: control.k8s.iaminyourpc.xyz
    http:
      paths:
      - pathType: Prefix
        path: /
        backend:
          service:
            name: fetch-ui
            port:
              number: 80
