---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: auth-immich
  namespace: monitoring
spec:
  refreshInterval: 5m0s
  secretStoreRef:
    name: home-vault
    kind: ClusterSecretStore
  target:
    name: auth-immich
    creationPolicy: Owner
  dataFrom:
  - extract:
      key: home/monitoring/auth-immich
      conversionStrategy: Default
      decodingStrategy: None
      metadataPolicy: None
